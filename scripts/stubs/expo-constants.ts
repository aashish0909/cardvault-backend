// Node stub for expo-constants (used only by scripts/*.test.ts).
// hostUri makes lib/config.ts point the relay at localhost.

export default {
  appOwnership: 'standalone',
  expoConfig: {
    hostUri: 'localhost:8081',
  },
};
